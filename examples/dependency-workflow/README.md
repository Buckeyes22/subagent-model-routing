# Dependency workflow example

Replace `replace-with/provider-model` with an OpenCode provider/model available in your installation, then run from this directory:

```bash
model-routing workflow run workflow.json --host copilot
```

The example is read-only. The second task receives only the first task's bounded stdout selection.
