# codex GPT-5.5 — capability card (seed example)

> Seed example — maintain via `/subagent-model-routing-claude:distill` and your own ledger.

- **Tier:** Strongest implementer in the roster (≥ GLM-5.2 Opus peer; reserved for hardest/critical + deepest review) *(seed ranking)*
- **Excels at:** hardest implementation, autonomous verify, deepest review
- **Struggles with:** (none confirmed at this tier yet — observations pending)
- **Operational caveats:** quota-costly — reserve for where it earns its wall-clock; `reasoning_effort` dial (low/medium/high); deep multi-file work routinely approaches the 20-min ceiling (split long jobs or raise SHIM_TIMEOUT_SECS deliberately)
- **Evidence:** seed default — replace with your own observations via `/subagent-model-routing-claude:distill`; observations accumulate in `~/.claude/subagent-model-routing/ledger/observations.jsonl`
- **Last distilled:** 2026-07-07 (seed)
